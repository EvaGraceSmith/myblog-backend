const people = ['tom', 'jerry', 'bob', 'alice'];
const ages = [20, 25, 30, 35];

console.log(people);

//{} is an empty object, people is the key (property), and people is the value. You can also use the shorthand below. (people: people = people)
module.exports = {people, ages};